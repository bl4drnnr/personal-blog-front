export enum ConfirmSubscriptionEnum {
  NEWSLETTER_NOT_FOUND = 'newsletters-not-found',
  SUBSCRIPTION_ALREADY_CONFIRMED = 'subscription-already-confirmed',
  SUBSCRIPTION_CONFIRMED = 'subscription-confirmed'
}
